<form method="post" action="{{ path('app_contact_status_delete', {'id': contact_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_status.id) }}">
    <button class="btn">Delete</button>
</form>
