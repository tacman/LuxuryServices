{% extends 'base.html.twig' %}

{% block title %}ApplicationStatus{% endblock %}

{% block body %}
    <h1>ApplicationStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ application_status.id }}</td>
            </tr>
            <tr>
                <th>StatusValue</th>
                <td>{{ application_status.statusValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_application_status_index') }}">back to list</a>

    <a href="{{ path('app_application_status_edit', {'id': application_status.id}) }}">edit</a>

    {{ include('application_status/_delete_form.html.twig') }}
{% endblock %}
